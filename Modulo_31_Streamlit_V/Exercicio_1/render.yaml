services:
  - type: web
    name: modulo-31-rfv-weslei
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app_RFV.py --server.port $PORT --server.address 0.0.0.0
